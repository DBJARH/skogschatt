{
  "rules": {
    "skogschatt": {
      "users": {
        ".read": "auth != null",
        ".write": false
      },
      "messages": {
        ".read": "auth != null",
        ".write": "auth != null",
        "$messageId": {
          ".validate": "newData.hasChildren(['user', 'text', 'ts'])"
        }
      }
    }
  }
}
